export enum AnimationClass {
  ANIMATE_IN = "AnimateIn",
  ANIMATE_OUT = "AnimateOut",
}

export enum Size {
  UNCHECKED,
  BIG = 0,
  SMALL = 1,
}

export enum Temperature {
  UNCHECKED,
  HOT = 0,
  ICE = 1,
}
